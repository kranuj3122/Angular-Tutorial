import { RedTagDirective } from './red-tag.directive';

describe('RedTagDirective', () => {
  it('should create an instance', () => {
    const directive = new RedTagDirective();
    expect(directive).toBeTruthy();
  });
});
